# Icon Machine i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 為 production UI(`index.html` + `main.js`)導入 zh-Hant / en 雙語切換。架構支援未來擴充更多語言;切語言以 dynamic DOM update 完成,不 reload、不重置 seed input、不重畫 batch grid;canvas 渲染與 regression 工具完全不動。

**Architecture:** 純 UI 層工具。三個新檔:`i18n/zh-Hant.js`、`i18n/en.js`(window 上各掛一個 `I18N_ZH_HANT` / `I18N_EN` 物件 — flat key→string 對照表)+ `i18n.js`(window.I18N IIFE,提供 `init` / `t` / `applyI18n` / `setLang` / `getLang`)。`index.html` 內既有 user-facing element 加 `data-i18n-key` / `data-i18n-attr-<name>` 屬性,`applyI18n()` 走 `querySelectorAll` 一次更新所有文字。`main.js` 啟動時 call `I18N.init()`,並在新增的 lang dropdown 綁 change handler。

**Tech Stack:** vanilla JS、HTML5 Canvas 2D、無 build tool、無 test framework。新增 Node script `scripts/check-i18n.mjs` 用於 static lang-table consistency check。

**Spec:** [`docs/superpowers/specs/2026-05-10-i18n-design.md`](../specs/2026-05-10-i18n-design.md)

---

## Project-Specific Notes

**Git repo:** 此專案是 git 倉。每個 task 結尾以 `git commit` 收束,訊息風格沿用既有(`feat(i18n): ...` / `chore: ...`)。

**No test framework:** 不引入任何 test 框架。驗證方式分兩類:
1. **Console-based**(資料 / 純函式):在瀏覽器打開 `index.html`,F12 開 devtools,Console 貼指定表達式,比對輸出。
2. **Visual / DOM-based**(UI 行為):開 `index.html`,看 UI 文字,或用 DevTools 檢視 DOM attribute / `<html lang>`。

每個 task 的 verify 步驟會明確說是哪一種、要看什麼。

**Snapshot 不應受影響:** i18n 純改 DOM 文字,不碰 canvas。Task 9 會跑 `npm run snapshot`,結果應該 byte-identical with baseline。**任一 baseline 變動是 bug**。

**Hard constraints(任何 task 都不能違反):**
- 不引入任何 frontend framework / npm dep / build step
- 不動 `regression.html`、`snapshots/`、`scripts/snapshot-regression.mjs`
- 不動 canvas 渲染相關檔案(`potion.js` / `sword.js` / `spear.js` / `palette.js` / `pixel-utils.js` / `random.js`)
- HTML 內保留 zh-Hant 文字當「i18n.js 沒載入時的 fallback」+ source-of-truth — 不要在 task 過程中把這些 text 改成空字串或 placeholder

**Critical naming:**
- 全域名稱:`window.I18N`(runtime)、`window.I18N_ZH_HANT`、`window.I18N_EN`(資料表)。三者都是 SCREAMING_SNAKE_CASE 變體,跟既有 `ITEM_TYPES` / `FAMILIES` / `WOOD_PALETTE` 風格一致。
- localStorage key:`'iconmachine.lang'`(string literal,不要拼錯)
- HTML attribute:`data-i18n-key="<key>"` 與 `data-i18n-attr-<attr>="<key>"`(attr 例:`placeholder` / `title`)。連字符段轉 dataset 是 camelCase:`data-i18n-attr-placeholder` → `el.dataset.i18nAttrPlaceholder`

**Translation key 命名(完整 list 在 spec §5,共 23 keys):** `<scope>.<purpose>` 小寫加 dot,例:`app.title` / `seed.placeholder` / `type.option.potion`。

---

## File Structure

| File | Status | Responsibility |
|---|---|---|
| `i18n/zh-Hant.js` | **NEW** | 掛 `window.I18N_ZH_HANT = { ...23 keys }` |
| `i18n/en.js` | **NEW** | 掛 `window.I18N_EN = { ...23 keys }` |
| `i18n.js` | **NEW** | `window.I18N` IIFE。`init` / `t` / `applyI18n` / `setLang` / `getLang` / `SUPPORTED` |
| `scripts/check-i18n.mjs` | **NEW** | Node script:diff lang tables + grep HTML keys + report missing/unused |
| `index.html` | **MODIFY** | 加 `<script src>`、加 lang dropdown、user-facing element 加 `data-i18n-*` 屬性、加 `.header-right` CSS |
| `main.js` | **MODIFY** | 加 `I18N.init()` 啟動 + 加 lang-select change event |
| `regression.html` | unchanged | 不動 |
| `random.js` / `palette.js` / `pixel-utils.js` / `potion.js` / `sword.js` / `spear.js` | unchanged | 不動 |
| `snapshots/`、`scripts/snapshot-regression.mjs` | unchanged | 不動 |

---

## Task 1: 建立 translation tables(`i18n/zh-Hant.js` + `i18n/en.js`)

**Files:**
- Create: `i18n/zh-Hant.js`
- Create: `i18n/en.js`
- Modify: `index.html`(只加兩條 `<script src>`,不動 DOM 內容)

此 task 結束時兩份 lang table 已掛上 `window`,但**還沒有任何使用者可見變化** — 因為還沒有 `applyI18n()`、HTML 也還沒加 `data-i18n-*` 屬性。

- [ ] **Step 1: 定義驗證目標**

開 `index.html`,Console 期望:

```js
// 兩個 table 都掛在 window 上
console.log(typeof window.I18N_ZH_HANT, typeof window.I18N_EN);
// 期望:object object

// 兩邊 key set 一致
const zhKeys = Object.keys(I18N_ZH_HANT).sort();
const enKeys = Object.keys(I18N_EN).sort();
console.log('zh keys:', zhKeys.length, 'en keys:', enKeys.length);
// 期望:zh keys: 23 en keys: 23

console.log('keys match:', JSON.stringify(zhKeys) === JSON.stringify(enKeys));
// 期望:keys match: true

// Spot check 幾個 key 的內容
console.log(I18N_ZH_HANT['app.title']);
// 期望:Icon Machine — 程序化像素圖示產生器
console.log(I18N_EN['app.title']);
// 期望:Icon Machine — Procedural Pixel Icon Generator

console.log(I18N_ZH_HANT['type.option.potion'], '|', I18N_EN['type.option.potion']);
// 期望:藥水 (potion) | Potion

console.log(I18N_ZH_HANT['seed.placeholder']);
// 期望:輸入種子,或留空隨機產生
console.log(I18N_EN['seed.placeholder']);
// 期望:Enter seed, or leave blank for random
```

- [ ] **Step 2: 建立 `i18n/` 目錄並寫 `i18n/zh-Hant.js`**

```bash
mkdir -p i18n
```

`i18n/zh-Hant.js` 完整內容:

```js
// i18n/zh-Hant.js
// 繁體中文翻譯表。新增 key 時必須同步在 i18n/en.js 加上同 key。

window.I18N_ZH_HANT = {
  'app.title': 'Icon Machine — 程序化像素圖示產生器',
  'app.tagline': '程序化像素圖示產生器',

  'preview.label': '// preview',
  'preview.scale': 'scale: 12x',
  'preview.format': 'format: png · transparent',

  'seed.label': '// seed',
  'seed.placeholder': '輸入種子,或留空隨機產生',
  'seed.reroll.title': '隨機新種子',

  'type.label': '// type',
  'type.option.any': '任意 (any)',
  'type.option.potion': '藥水 (potion)',
  'type.option.sword': '劍 (sword)',
  'type.option.spear': '長矛 (spear)',

  'size.label': '// size',

  'generate.button': '▸ generate',
  'download.button': '⇩ png',

  'tip.text': '提示:同樣的種子永遠產生同樣的圖示。下方批次預覽中,點任何一張圖示即可載入。',

  'batch.heading.before': 'batch',
  'batch.heading.after': 'preview',
  'batch.hint': '點任一格載入該種子',

  'footer.skeleton': 'Icon Machine — 第一版骨架',
  'footer.inspired': 'Inspired by',
  'footer.todo': '繪圖規則待擴充',
};
```

- [ ] **Step 3: 寫 `i18n/en.js`**

```js
// i18n/en.js
// English translation table. Adding a key here requires the same key in i18n/zh-Hant.js.

window.I18N_EN = {
  'app.title': 'Icon Machine — Procedural Pixel Icon Generator',
  'app.tagline': 'Procedural pixel icon generator',

  'preview.label': '// preview',
  'preview.scale': 'scale: 12x',
  'preview.format': 'format: png · transparent',

  'seed.label': '// seed',
  'seed.placeholder': 'Enter seed, or leave blank for random',
  'seed.reroll.title': 'New random seed',

  'type.label': '// type',
  'type.option.any': 'Any',
  'type.option.potion': 'Potion',
  'type.option.sword': 'Sword',
  'type.option.spear': 'Spear',

  'size.label': '// size',

  'generate.button': '▸ generate',
  'download.button': '⇩ png',

  'tip.text': 'Tip: the same seed always yields the same icon. Click any cell in the batch preview to load it.',

  'batch.heading.before': 'batch',
  'batch.heading.after': 'preview',
  'batch.hint': 'Click any cell to load that seed',

  'footer.skeleton': 'Icon Machine — first skeleton',
  'footer.inspired': 'Inspired by',
  'footer.todo': 'More drawing rules to come',
};
```

- [ ] **Step 4: 在 `index.html` 加兩條 `<script src>`**

找到 index.html 底部的 script 載入區段:

```html
  <script src="random.js"></script>
  <script src="palette.js"></script>
  <script src="pixel-utils.js"></script>
  <script src="potion.js"></script>
  <script src="sword.js"></script>
  <script src="spear.js"></script>
  <script src="main.js"></script>
```

改成(在 `spear.js` 後、`main.js` 前插入兩條):

```html
  <script src="random.js"></script>
  <script src="palette.js"></script>
  <script src="pixel-utils.js"></script>
  <script src="potion.js"></script>
  <script src="sword.js"></script>
  <script src="spear.js"></script>
  <script src="i18n/zh-Hant.js"></script>
  <script src="i18n/en.js"></script>
  <script src="main.js"></script>
```

(注意:**還沒**加 `i18n.js` 的 script tag — 因為 i18n.js 還沒寫。Task 2 才加。)

- [ ] **Step 5: 開啟 index.html、跑驗證表達式**

開啟 `index.html`(直接 file:// 雙擊或 `open index.html`),F12 開 Console,把 Step 1 那組表達式逐句貼進去跑,比對輸出。

**全部期望命中** → 進下一步。
**任一輸出不符** → 修對應 key,再跑一次。

- [ ] **Step 6: 確認 UI 視覺零變化**

肉眼檢查 `index.html`:應該跟 task 開始前一模一樣(zh-Hant 文字)、batch grid 仍能 render、generate / reroll / download 按鈕仍能用。**任何視覺變化都是 bug**(本 task 不應改動使用者可見內容)。

- [ ] **Step 7: Commit**

```bash
git add i18n/zh-Hant.js i18n/en.js index.html
git commit -m "$(cat <<'EOF'
feat(i18n): add zh-Hant and en translation tables (23 keys)

Tables registered as window.I18N_ZH_HANT / window.I18N_EN.
index.html script-loaded but not yet consumed — runtime in next commit.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 建立 `i18n.js` runtime 骨架(`t()` + `getLang()` + `SUPPORTED`)

**Files:**
- Create: `i18n.js`
- Modify: `index.html`(加一條 `<script src="i18n.js">`)

Task 2 只實作 `t()` lookup 與 fallback chain。`currentLang` 暫時 hardcode 為 `'zh-Hant'`,沒有 lang resolution、沒有 setLang、沒有 applyI18n。Task 2 結束時 console 可呼叫 `I18N.t('app.title')` 拿到 zh-Hant 字串,但 UI 文字仍由 HTML 寫死提供(不變)。

- [ ] **Step 1: 定義驗證目標**

開 `index.html`,Console 期望:

```js
// I18N 物件 surface 已展開
console.log(typeof I18N, Object.keys(I18N).sort());
// 期望:object ['SUPPORTED', 'getLang', 't']

// 預設語言
console.log(I18N.getLang());
// 期望:zh-Hant

console.log(I18N.SUPPORTED);
// 期望:['zh-Hant', 'en']

// t() 命中 current lang(zh-Hant)
console.log(I18N.t('app.title'));
// 期望:Icon Machine — 程序化像素圖示產生器
console.log(I18N.t('type.option.potion'));
// 期望:藥水 (potion)

// t() missing key fallback:回 key 本身 + console.warn
console.log(I18N.t('does.not.exist'));
// 期望:does.not.exist
// + console 應出現一則 warn:[i18n] missing key: does.not.exist (lang=zh-Hant)
```

- [ ] **Step 2: 寫 `i18n.js`**

完整內容(注意:Task 2 版本只實作 §3.3 t() + §3 surface 的 getLang/SUPPORTED;其他成員 init/applyI18n/setLang 後續 task 加入):

```js
// i18n.js
// Runtime for translating UI strings. Reads window.I18N_ZH_HANT / window.I18N_EN
// (loaded by i18n/zh-Hant.js and i18n/en.js) and exposes window.I18N.

window.I18N = (function () {
  const SUPPORTED = ['zh-Hant', 'en'];
  const DEFAULT_LANG = 'zh-Hant';

  const TABLES = {
    'zh-Hant': window.I18N_ZH_HANT,
    'en':      window.I18N_EN,
  };

  let currentLang = DEFAULT_LANG;

  function t(key) {
    const table = TABLES[currentLang];
    if (table && key in table) return table[key];
    if (currentLang !== 'zh-Hant' && TABLES['zh-Hant'] && key in TABLES['zh-Hant']) {
      return TABLES['zh-Hant'][key];
    }
    console.warn(`[i18n] missing key: ${key} (lang=${currentLang})`);
    return key;
  }

  return {
    SUPPORTED,
    getLang: () => currentLang,
    t,
  };
})();
```

- [ ] **Step 3: 在 `index.html` 加 `<script src="i18n.js">`**

找到 Task 1 寫進去的 script 區段:

```html
  <script src="i18n/zh-Hant.js"></script>
  <script src="i18n/en.js"></script>
  <script src="main.js"></script>
```

在 `i18n/en.js` 後、`main.js` 前插一條:

```html
  <script src="i18n/zh-Hant.js"></script>
  <script src="i18n/en.js"></script>
  <script src="i18n.js"></script>
  <script src="main.js"></script>
```

順序 critical:`i18n/zh-Hant.js` + `i18n/en.js` 必須先載入(因為 i18n.js 內 `TABLES` 物件初始化時讀 `window.I18N_ZH_HANT` / `window.I18N_EN`,他們必須已存在)。

- [ ] **Step 4: 跑驗證表達式**

開啟 `index.html`(若已開,reload),Console 跑 Step 1 表達式,比對輸出。

- [ ] **Step 5: 確認 UI 視覺零變化**

肉眼檢查 index.html UI:跟 task 開始前一模一樣。Task 2 還沒實作 applyI18n,UI 文字仍由 HTML 寫死。

- [ ] **Step 6: Commit**

```bash
git add i18n.js index.html
git commit -m "$(cat <<'EOF'
feat(i18n): add window.I18N runtime skeleton with t() lookup

t() implements 3-tier fallback: current lang > zh-Hant > key string + warn.
init/applyI18n/setLang stubs added in subsequent commits.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 在 `index.html` 加 `data-i18n-*` 屬性(無 JS 改動)

**Files:**
- Modify: `index.html`

此 task 不改 JS、不改 CSS、不引入 lang dropdown。**只**對 user-facing element 加 `data-i18n-key` 或 `data-i18n-attr-<name>`,讓未來 `applyI18n()` 能 query 到。HTML 內既有 zh-Hant 文字保留不動(當 fallback / source-of-truth)。

- [ ] **Step 1: 定義驗證目標**

開啟 `index.html`,Console 期望:

```js
// 文字 element 共 22 個有 data-i18n-key
const keyEls = document.querySelectorAll('[data-i18n-key]');
console.log('elements with data-i18n-key:', keyEls.length);
// 期望:22

// 屬性 element 各自:placeholder(seed input)1 個、title(reroll button)1 個
console.log('placeholder targets:', document.querySelectorAll('[data-i18n-attr-placeholder]').length);
// 期望:1
console.log('title targets:', document.querySelectorAll('[data-i18n-attr-title]').length);
// 期望:1

// 蒐集所有 key 名稱,跟翻譯表比對
const usedKeys = new Set();
document.querySelectorAll('[data-i18n-key]').forEach(el => usedKeys.add(el.dataset.i18nKey));
document.querySelectorAll('[data-i18n-attr-placeholder]').forEach(el => usedKeys.add(el.dataset.i18nAttrPlaceholder));
document.querySelectorAll('[data-i18n-attr-title]').forEach(el => usedKeys.add(el.dataset.i18nAttrTitle));
console.log('used keys:', usedKeys.size, [...usedKeys].sort());
// 期望:used keys: 23 加排序 list

// 每一個 used key 必須在 zh-Hant 表內
const zhKeys = new Set(Object.keys(I18N_ZH_HANT));
const missingInTable = [...usedKeys].filter(k => !zhKeys.has(k));
console.log('missing in I18N_ZH_HANT:', missingInTable);
// 期望:missing in I18N_ZH_HANT: []

// 反向:每個 table key 都應該有 element 用(MVP 應該全用上)
const zhKeysList = [...zhKeys];
const unusedInHTML = zhKeysList.filter(k => !usedKeys.has(k));
console.log('defined but unused:', unusedInHTML);
// 期望:defined but unused: []
```

- [ ] **Step 2: 編輯 `index.html` 給 `<title>` 加 attribute**

找:
```html
  <title>Icon Machine — 程序化像素圖示產生器</title>
```

改:
```html
  <title data-i18n-key="app.title">Icon Machine — 程序化像素圖示產生器</title>
```

- [ ] **Step 3: tagline 加 attribute**

找:
```html
      <div class="tagline">
        程序化像素圖示產生器 <span class="blink"></span>
      </div>
```

改成:
```html
      <div class="tagline">
        <span data-i18n-key="app.tagline">程序化像素圖示產生器</span> <span class="blink"></span>
      </div>
```

(把純文字包進 `<span data-i18n-key>` — `applyI18n()` 用 `el.textContent =` 寫,如果裸文字直接掛 `<div data-i18n-key>` 會把 `<span class="blink">` 一起蓋掉,所以要包一層。)

- [ ] **Step 4: preview 區三條(label / scale / format)加 attribute**

找:
```html
        <div class="preview-label">// preview</div>
        <div class="preview-frame">
          <canvas id="preview-canvas" width="384" height="384"></canvas>
        </div>
        <div class="preview-info">
          <span>scale: 12x</span>
          <span>format: png · transparent</span>
        </div>
```

改成:
```html
        <div class="preview-label" data-i18n-key="preview.label">// preview</div>
        <div class="preview-frame">
          <canvas id="preview-canvas" width="384" height="384"></canvas>
        </div>
        <div class="preview-info">
          <span data-i18n-key="preview.scale">scale: 12x</span>
          <span data-i18n-key="preview.format">format: png · transparent</span>
        </div>
```

- [ ] **Step 5: seed 控制組(label / placeholder / reroll title)加 attribute**

找:
```html
        <div class="control-group">
          <label for="seed-input">// seed</label>
          <div class="seed-row">
            <input
              type="text"
              id="seed-input"
              placeholder="輸入種子,或留空隨機產生"
              autocomplete="off"
              spellcheck="false"
            />
            <button id="reroll-seed-btn" class="icon-btn" title="隨機新種子">⟳</button>
          </div>
        </div>
```

改成:
```html
        <div class="control-group">
          <label for="seed-input" data-i18n-key="seed.label">// seed</label>
          <div class="seed-row">
            <input
              type="text"
              id="seed-input"
              data-i18n-attr-placeholder="seed.placeholder"
              placeholder="輸入種子,或留空隨機產生"
              autocomplete="off"
              spellcheck="false"
            />
            <button id="reroll-seed-btn" class="icon-btn" data-i18n-attr-title="seed.reroll.title" title="隨機新種子">⟳</button>
          </div>
        </div>
```

- [ ] **Step 6: type select 控制組(label + 4 個 option)加 attribute**

找:
```html
          <div class="control-group">
            <label for="type-select">// type</label>
            <select id="type-select">
              <option value="any">任意 (any)</option>
              <option value="potion">藥水 (potion)</option>
              <option value="sword">劍 (sword)</option>
              <option value="spear">長矛 (spear)</option>
            </select>
          </div>
```

改成:
```html
          <div class="control-group">
            <label for="type-select" data-i18n-key="type.label">// type</label>
            <select id="type-select">
              <option value="any" data-i18n-key="type.option.any">任意 (any)</option>
              <option value="potion" data-i18n-key="type.option.potion">藥水 (potion)</option>
              <option value="sword" data-i18n-key="type.option.sword">劍 (sword)</option>
              <option value="spear" data-i18n-key="type.option.spear">長矛 (spear)</option>
            </select>
          </div>
```

- [ ] **Step 7: size select label 加 attribute**

找:
```html
          <div class="control-group">
            <label for="size-select">// size</label>
            <select id="size-select">
              <option value="32">32 × 32</option>
              <option value="16">16 × 16</option>
            </select>
          </div>
```

改成(只動 label,option 是純尺寸不翻):
```html
          <div class="control-group">
            <label for="size-select" data-i18n-key="size.label">// size</label>
            <select id="size-select">
              <option value="32">32 × 32</option>
              <option value="16">16 × 16</option>
            </select>
          </div>
```

- [ ] **Step 8: generate / download 兩顆按鈕加 attribute**

找:
```html
        <div class="button-row">
          <button id="generate-btn" class="primary">▸ generate</button>
          <button id="download-btn">⇩ png</button>
        </div>
```

改成:
```html
        <div class="button-row">
          <button id="generate-btn" class="primary" data-i18n-key="generate.button">▸ generate</button>
          <button id="download-btn" data-i18n-key="download.button">⇩ png</button>
        </div>
```

- [ ] **Step 9: 提示 div 加 attribute**

找:
```html
        <div style="font-size: 11px; color: var(--ink-dim); line-height: 1.6;">
          提示:同樣的種子永遠產生同樣的圖示。下方批次預覽中,點任何一張圖示即可載入。
        </div>
```

改成:
```html
        <div style="font-size: 11px; color: var(--ink-dim); line-height: 1.6;" data-i18n-key="tip.text">
          提示:同樣的種子永遠產生同樣的圖示。下方批次預覽中,點任何一張圖示即可載入。
        </div>
```

- [ ] **Step 10: batch heading + hint 加 attribute**

找:
```html
      <div class="grid-section-header">
        <h2>batch<span class="accent">_</span>preview</h2>
        <div class="hint">點任一格載入該種子</div>
      </div>
```

改成(把 h2 內的 `batch` / `preview` 各自包進 `<span>` — 中間 `_` 帶 accent 類別不動):
```html
      <div class="grid-section-header">
        <h2><span data-i18n-key="batch.heading.before">batch</span><span class="accent">_</span><span data-i18n-key="batch.heading.after">preview</span></h2>
        <div class="hint" data-i18n-key="batch.hint">點任一格載入該種子</div>
      </div>
```

- [ ] **Step 11: footer 三段加 attribute**

找:
```html
    <footer>
      Icon Machine — 第一版骨架  ·  Inspired by
      <a href="https://bmaczero.itch.io/icon-machine" target="_blank" rel="noopener">Brian MacIntosh</a>
      ·  繪圖規則待擴充
    </footer>
```

改成:
```html
    <footer>
      <span data-i18n-key="footer.skeleton">Icon Machine — 第一版骨架</span>  ·  <span data-i18n-key="footer.inspired">Inspired by</span>
      <a href="https://bmaczero.itch.io/icon-machine" target="_blank" rel="noopener">Brian MacIntosh</a>
      ·  <span data-i18n-key="footer.todo">繪圖規則待擴充</span>
    </footer>
```

(人名 Brian MacIntosh 不翻,維持在 anchor 內。)

- [ ] **Step 12: 跑驗證表達式**

Reload `index.html`,跑 Step 1 表達式。**全部命中** → 進下一步。

特別注意 `defined but unused: []` — 若任何 zh-Hant key 沒被 HTML 用到,代表 step 2-11 漏改一處 element,回去找。

- [ ] **Step 13: 肉眼確認 UI 沒變**

UI 文字應跟 task 開始前一致(zh-Hant)。Browser 不會渲染 `data-*` attribute、也不會吃掉 element 內既有 textContent,所以加 attribute 對使用者透明。

特別檢查:
- `<title>` tab 標題仍正常
- seed input placeholder 仍顯示「輸入種子,或留空隨機產生」
- reroll button hover title 仍顯示「隨機新種子」
- type-select 4 個選項 textContent 仍正確
- footer 三段 + 中間 anchor 連結排列仍如舊

- [ ] **Step 14: Commit**

```bash
git add index.html
git commit -m "$(cat <<'EOF'
feat(i18n): annotate index.html user-facing elements with data-i18n-* attrs

22 elements get data-i18n-key, 1 input gets data-i18n-attr-placeholder,
1 button gets data-i18n-attr-title. No JS or CSS change; HTML zh-Hant text
preserved as fallback. applyI18n() consumer added in next commit.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 實作 `applyI18n()` + main.js 啟動 call `I18N.init()`

**Files:**
- Modify: `i18n.js`(加 `applyI18n` + `init` 方法,export 出來)
- Modify: `main.js`(在啟動序加 `I18N.init()`)

Task 4 結束時 i18n 對 DOM 生效。`I18N.init()` 仍 hardcode `currentLang = 'zh-Hant'`(沒接 lang resolution),但 DOM 文字會由 i18n 機制重設,使用者**不應該**看到任何視覺差異(因為 HTML 內 fallback 文字跟 zh-Hant table 內字串一致)。

- [ ] **Step 1: 定義驗證目標**

開 `index.html`,Console 期望:

```js
// init 已存在
console.log(typeof I18N.init, typeof I18N.applyI18n);
// 期望:function function

// <html lang> 已被 I18N.init() 設好
console.log(document.documentElement.lang);
// 期望:zh-Hant

// 隨意一個 element textContent 應跟 zh-Hant table 一致
const tip = document.querySelector('[data-i18n-key="tip.text"]');
console.log(tip.textContent === I18N_ZH_HANT['tip.text']);
// 期望:true(空白 trim 過後比對)— 注意原 HTML 文字外層有縮排空白,init 後會 overwrite 為精確 string
console.log(JSON.stringify(tip.textContent));
// 期望:精確等於 zh-Hant 表內字串(不含換行縮排)

// placeholder 已被 I18N.init() 套上
console.log(document.getElementById('seed-input').placeholder);
// 期望:輸入種子,或留空隨機產生

// type-select 第一個 option textContent
console.log(document.querySelector('#type-select option[value="potion"]').textContent);
// 期望:藥水 (potion)

// 手動 call applyI18n() 應 idempotent
I18N.applyI18n();
console.log('idempotent ok');
// 期望:無 error,DOM 不變
```

- [ ] **Step 2: 在 `i18n.js` 加 `applyI18n()` + `init()`**

找:
```js
  function t(key) {
    ...
  }

  return {
    SUPPORTED,
    getLang: () => currentLang,
    t,
  };
```

改成(在 t 之後、return 之前插入 applyI18n + init,並 export):

```js
  function t(key) {
    const table = TABLES[currentLang];
    if (table && key in table) return table[key];
    if (currentLang !== 'zh-Hant' && TABLES['zh-Hant'] && key in TABLES['zh-Hant']) {
      return TABLES['zh-Hant'][key];
    }
    console.warn(`[i18n] missing key: ${key} (lang=${currentLang})`);
    return key;
  }

  // MVP 只處理 placeholder / title / aria-label;新增屬性在這個 array 加一筆即可
  const ATTR_NAMES = ['placeholder', 'title', 'aria-label'];

  function applyI18n(root) {
    root = root || document;

    // 1. <html lang>
    document.documentElement.lang = currentLang;

    // 2. textContent
    root.querySelectorAll('[data-i18n-key]').forEach((el) => {
      el.textContent = t(el.dataset.i18nKey);
    });

    // 3. attribute
    for (const attr of ATTR_NAMES) {
      const datasetProp = 'i18nAttr' + attr.replace(/(^|-)([a-z])/g, (_, _dash, ch) => ch.toUpperCase());
      // attr='placeholder' → datasetProp='i18nAttrPlaceholder'
      // attr='aria-label' → datasetProp='i18nAttrAriaLabel'
      const sel = `[data-i18n-attr-${attr}]`;
      root.querySelectorAll(sel).forEach((el) => {
        const key = el.dataset[datasetProp];
        if (key) el.setAttribute(attr, t(key));
      });
    }
  }

  function init() {
    // Task 5 將擴充為 resolveInitialLang chain;Task 4 暫時固定 zh-Hant
    currentLang = DEFAULT_LANG;
    applyI18n();
  }

  return {
    SUPPORTED,
    getLang: () => currentLang,
    t,
    applyI18n,
    init,
  };
```

> **dataset key 推導註解:** HTML5 dataset 會把 `data-i18n-attr-placeholder` 自動轉成 `dataset.i18nAttrPlaceholder`(連字符後一字符大寫)。若日後新增 `aria-label`,寫 `data-i18n-attr-aria-label` → `dataset.i18nAttrAriaLabel`。上面的 regex 把 `aria-label` → `AriaLabel` 跟 dataset 對齊。

- [ ] **Step 3: 在 `main.js` 加 `I18N.init()` 啟動**

找 main.js 末尾的啟動序:

```js
// =============================================================
// 啟動
// =============================================================
seedInput.value = generateRandomSeed();
generate();
renderBatchGrid();
```

改成(在 `seedInput.value =` 之前插入 `I18N.init()`):

```js
// =============================================================
// 啟動
// =============================================================
I18N.init();
seedInput.value = generateRandomSeed();
generate();
renderBatchGrid();
```

- [ ] **Step 4: 跑驗證表達式 + UI 視覺檢查**

Reload `index.html`:
1. Console 跑 Step 1 表達式,全命中。
2. UI 視覺:文字仍是 zh-Hant、跟 task 開始前一模一樣。**任何文字變化都是 bug**(可能是 zh-Hant table 跟 HTML fallback 文字不一致)。
3. 把 type-select 打開,4 個選項仍是「任意 (any)」/「藥水 (potion)」/「劍 (sword)」/「長矛 (spear)」。

- [ ] **Step 5: 確認 batch grid + canvas 不受影響**

UI:點 generate 按鈕、點 batch grid 任一格,canvas 渲染應正常。**Canvas 像素應跟 task 開始前完全一致**(i18n 不該碰 canvas)。

- [ ] **Step 6: Commit**

```bash
git add i18n.js main.js
git commit -m "$(cat <<'EOF'
feat(i18n): wire applyI18n() to DOM via I18N.init() at startup

applyI18n updates <html lang>, [data-i18n-key].textContent, and
[data-i18n-attr-placeholder|title|aria-label] attributes. Lang resolution
still hardcoded to zh-Hant; chain added in next commit.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 實作 `resolveInitialLang()` 完整 chain(URL → localStorage → navigator → default)

**Files:**
- Modify: `i18n.js`(`init` 改 call `resolveInitialLang()` 取代 hardcode)

Task 5 結束時開頁面決定的語言會走 4 段 chain。手動驗證每一段:URL `?lang=en` / localStorage / navigator.language / fallback。

- [ ] **Step 1: 定義驗證目標**

每一情境都要清乾淨 localStorage 再開新分頁(避免被前一輪測試殘留污染)。所有測試從一致的 baseline 開始。

清 localStorage 的 console 指令(每輪測試前都跑一次):
```js
localStorage.removeItem('iconmachine.lang');
location.reload();
```

**測試矩陣:**

| 情境 | URL | localStorage | navigator.language | 期望 currentLang | localStorage 寫入後值 |
|---|---|---|---|---|---|
| A. 全空 + zh navigator | `index.html` | (empty) | `zh-TW` | `zh-Hant` | (unchanged) |
| B. 全空 + en navigator | `index.html` | (empty) | `en-US` | `en` | (unchanged) |
| C. 全空 + 其他 navigator | `index.html` | (empty) | `fr-FR` | `zh-Hant` (fallback) | (unchanged) |
| D. URL valid 覆蓋 | `index.html?lang=en` | (empty) | `zh-TW` | `en` | `en` |
| E. URL invalid 跳過 | `index.html?lang=fr` | (empty) | `zh-TW` | `zh-Hant` | (unchanged,**重要**) |
| F. localStorage 有值 | `index.html` | `en` | `zh-TW` | `en` | (unchanged) |
| G. URL > localStorage | `index.html?lang=zh-Hant` | `en` | `zh-TW` | `zh-Hant` | `zh-Hant` |
| H. URL invalid + localStorage 有值 | `index.html?lang=fr` | `en` | `zh-TW` | `en`(走 localStorage) | `en` |

(navigator.language 在 DevTools 不能簡單覆寫;對應 A/B/C 用「相信 navigator.language 報的值」即可,實際機器是 zh-TW 跑出 zh-Hant 即驗到 A;Chrome 上可用 `Ctrl+Shift+P → Sensors → Language` 模擬,但本 task 不要求做 navigator override。**A/B/C 至少測自己 navigator 對應的那一項**,其他依信任條款。)

- [ ] **Step 2: 在 `i18n.js` 加 `STORAGE_KEY` 常數 + `resolveInitialLang()`**

找:
```js
  const SUPPORTED = ['zh-Hant', 'en'];
  const DEFAULT_LANG = 'zh-Hant';

  const TABLES = {
```

改成(在 DEFAULT_LANG 後插入 STORAGE_KEY):
```js
  const SUPPORTED = ['zh-Hant', 'en'];
  const DEFAULT_LANG = 'zh-Hant';
  const STORAGE_KEY = 'iconmachine.lang';

  const TABLES = {
```

接著找 t 函式之前(或 init 之前任何位置):
```js
  function t(key) {
```

在它前面插入 `resolveInitialLang`:

```js
  function readLangFromURL() {
    try {
      const params = new URLSearchParams(window.location.search);
      const v = params.get('lang');
      if (v && SUPPORTED.includes(v)) return v;
    } catch (_) {}
    return null;
  }

  function readLangFromStorage() {
    try {
      const v = localStorage.getItem(STORAGE_KEY);
      if (v && SUPPORTED.includes(v)) return v;
    } catch (_) {}
    return null;
  }

  function readLangFromNavigator() {
    try {
      const nav = (navigator.language || '').toLowerCase();
      if (nav.startsWith('zh')) return 'zh-Hant';
      if (nav.startsWith('en')) return 'en';
    } catch (_) {}
    return null;
  }

  function resolveInitialLang() {
    // URL 命中時,順手把它寫進 localStorage(讓分享連結進來後會被記住)
    const urlLang = readLangFromURL();
    if (urlLang) {
      try { localStorage.setItem(STORAGE_KEY, urlLang); } catch (_) {}
      return urlLang;
    }
    return readLangFromStorage()
        || readLangFromNavigator()
        || DEFAULT_LANG;
  }

  function t(key) {
```

- [ ] **Step 3: 改 `init()` 用 `resolveInitialLang()`**

找:
```js
  function init() {
    // Task 5 將擴充為 resolveInitialLang chain;Task 4 暫時固定 zh-Hant
    currentLang = DEFAULT_LANG;
    applyI18n();
  }
```

改成:
```js
  function init() {
    currentLang = resolveInitialLang();
    applyI18n();
  }
```

- [ ] **Step 4: 跑情境 A — 全空 default**

```js
localStorage.removeItem('iconmachine.lang');
```

開 `index.html`(沒有 query string),Console:
```js
console.log(I18N.getLang(), document.documentElement.lang, localStorage.getItem('iconmachine.lang'));
// 期望(假設 navigator.language 開頭是 zh):zh-Hant zh-Hant null
// 期望(假設 navigator.language 開頭是 en):en en null
// 期望(其他):zh-Hant zh-Hant null
```

- [ ] **Step 5: 跑情境 D — URL valid**

```js
localStorage.removeItem('iconmachine.lang');
```

開 `index.html?lang=en`,Console:
```js
console.log(I18N.getLang(), document.documentElement.lang, localStorage.getItem('iconmachine.lang'));
// 期望:en en en
```

UI 視覺:tagline / labels / buttons / placeholder 應全顯示 en。

- [ ] **Step 6: 跑情境 E — URL invalid 不污染 localStorage**

```js
localStorage.removeItem('iconmachine.lang');
```

開 `index.html?lang=fr`,Console:
```js
console.log(I18N.getLang(), localStorage.getItem('iconmachine.lang'));
// 期望:zh-Hant null   ← null 是 critical;若有值代表 invalid 被持久化,bug
```

- [ ] **Step 7: 跑情境 F — localStorage 持久化**

```js
localStorage.setItem('iconmachine.lang', 'en');
```

開 `index.html`(無 query string),reload:
```js
console.log(I18N.getLang(), localStorage.getItem('iconmachine.lang'));
// 期望:en en
```

UI 視覺:en。

- [ ] **Step 8: 跑情境 G — URL 蓋 localStorage**

(承前 localStorage = 'en')開 `index.html?lang=zh-Hant`:
```js
console.log(I18N.getLang(), localStorage.getItem('iconmachine.lang'));
// 期望:zh-Hant zh-Hant   ← URL 命中後把 localStorage 蓋成新值
```

- [ ] **Step 9: 清完狀態 + 再開一次確認沒副作用**

```js
localStorage.removeItem('iconmachine.lang');
```

開 `index.html`,確認回到情境 A。

- [ ] **Step 10: Commit**

```bash
git add i18n.js
git commit -m "$(cat <<'EOF'
feat(i18n): implement resolveInitialLang chain (URL > localStorage > navigator)

URL ?lang= valid → adopt + persist to localStorage; invalid → skip without
polluting storage. localStorage / navigator-prefix / DEFAULT_LANG fallback.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: 實作 `validateTables()` 早期警告

**Files:**
- Modify: `i18n.js`(加 `validateTables`、`init` 內 call)

`validateTables` 對 `I18N_ZH_HANT` 跟 `I18N_EN` 的 key set 做 diff,缺 key 時 console.error 輸出兩邊缺什麼。**不 throw**(避免 prod 開不了)。給開發者用,使用者沒 console 不會看到。

- [ ] **Step 1: 定義驗證目標**

(乾淨情境)開啟 `index.html`,Console 期望:
```js
// 表完整時,init 跑完應無 mismatch error
// (應該看不到 [i18n] table mismatch 訊息;先把 console clear 再 reload 才好觀察)
```

(故意破壞)在 Console 直接刪一個 en key 模擬不一致(臨時 demo,不修進原始檔):

```js
// 暫時刪除 en 的一個 key,然後手動跑一次 validate
const removed = I18N_EN['app.tagline'];
delete I18N_EN['app.tagline'];

// 用一個臨時方法觸發 validate(我們會把 validate 放在 init 中,但也在 IIFE 內為自己留 reference)
// 最簡單方式:reload — i18n.js 重新跑時 init 會 call validateTables
location.reload();
// reload 後 Console 應出現:
// [i18n] table mismatch:
//   Missing in en:      ['app.tagline']
//   Missing in zh-Hant: []

// 修回去:
I18N_EN['app.tagline'] = removed; // (reload 後 table 已恢復原狀,這條只是清楚展示 contract)
```

(注意:reload 後因為 i18n/en.js 重新載入,en 表會回到完整狀態 — 所以不需要手動修回。)

- [ ] **Step 2: 在 `i18n.js` 加 `validateTables()`**

在 `resolveInitialLang` 函式之後、`t` 之前(或任何 IIFE 內位置都可以),插入:

```js
  function validateTables() {
    const langs = Object.keys(TABLES);
    if (langs.length < 2) return; // single lang 沒得比

    const allKeys = new Set();
    for (const lang of langs) {
      const tbl = TABLES[lang];
      if (!tbl) continue;
      Object.keys(tbl).forEach((k) => allKeys.add(k));
    }

    const missing = {};
    let anyMissing = false;
    for (const lang of langs) {
      const tbl = TABLES[lang] || {};
      const lacks = [];
      allKeys.forEach((k) => { if (!(k in tbl)) lacks.push(k); });
      if (lacks.length > 0) {
        missing[lang] = lacks;
        anyMissing = true;
      }
    }

    if (anyMissing) {
      const lines = ['[i18n] table mismatch:'];
      for (const lang of langs) {
        lines.push(`  Missing in ${lang}: [${(missing[lang] || []).join(', ')}]`);
      }
      console.error(lines.join('\n'));
    }
  }
```

- [ ] **Step 3: 改 `init()` 在 `applyI18n()` 之前 call `validateTables()`**

找:
```js
  function init() {
    currentLang = resolveInitialLang();
    applyI18n();
  }
```

改成:
```js
  function init() {
    currentLang = resolveInitialLang();
    validateTables();
    applyI18n();
  }
```

- [ ] **Step 4: Reload 跑乾淨情境**

開 `index.html`,Console 應**無** `[i18n] table mismatch` error。若有 → 表示 zh-Hant 跟 en 哪個 key 漏寫,回 Task 1 修正。

- [ ] **Step 5: 跑故意破壞情境**

把 `i18n/en.js` 內的 `'app.tagline': '...'` 那行**暫時**註解掉,reload `index.html`。Console 應出現:
```
[i18n] table mismatch:
  Missing in zh-Hant: []
  Missing in en: [app.tagline]
```

(順序可能反過來,看 langs 陣列;只要 `Missing in en` 列出 `app.tagline` 即 PASS。)

UI 視覺:`app.tagline` 那行(tagline span)會顯示 zh-Hant fallback「程序化像素圖示產生器」(因 currentLang=en 但 fallback 到 zh-Hant)— 行為符合 §3.3 t() fallback。

- [ ] **Step 6: 把破壞還原**

把 `i18n/en.js` 註解掉的那行恢復回來(取消 //)。reload,Console 再次乾淨無 error。

- [ ] **Step 7: Commit**

```bash
git add i18n.js
git commit -m "$(cat <<'EOF'
feat(i18n): add validateTables() dev-time integrity check

Diffs key sets across all registered tables, console.error on mismatch.
Does not throw — app keeps running with t() 3-tier fallback.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 實作 `setLang()` + 加 lang dropdown UI + main.js 接 change event

**Files:**
- Modify: `i18n.js`(加 `setLang`)
- Modify: `index.html`(加 lang dropdown + `.header-right` CSS)
- Modify: `main.js`(綁 lang-select change event)

Task 7 結束時切語言 UX 完整可用。

- [ ] **Step 1: 定義驗證目標**

開 `index.html`,Console 期望:

```js
// setLang 已 export
console.log(typeof I18N.setLang);
// 期望:function

// 切到 en
I18N.setLang('en');
console.log(I18N.getLang(), document.documentElement.lang, localStorage.getItem('iconmachine.lang'));
// 期望:en en en

// UI 文字應已即時換為 en — 隨意檢查幾處
console.log(document.querySelector('[data-i18n-key="app.tagline"]').textContent);
// 期望:Procedural pixel icon generator
console.log(document.getElementById('seed-input').placeholder);
// 期望:Enter seed, or leave blank for random

// 切回 zh-Hant
I18N.setLang('zh-Hant');
console.log(I18N.getLang(), document.documentElement.lang, localStorage.getItem('iconmachine.lang'));
// 期望:zh-Hant zh-Hant zh-Hant

// invalid 防呆
I18N.setLang('fr');
console.log(I18N.getLang(), localStorage.getItem('iconmachine.lang'));
// 期望:zh-Hant zh-Hant   ← 不變、localStorage 不被污染

// no-op:setLang 同一個 lang
I18N.setLang('zh-Hant');
console.log('no-op ok');
// 期望:無 error,DOM 未額外觸發更新
```

UI 視覺:header 應出現一個小 dropdown,列出「繁體中文 / English」。手動切換應即時換語言,seed input 內容不變、batch grid 不重畫。

- [ ] **Step 2: 在 `i18n.js` 加 `setLang()`**

找(t 之後、或 applyI18n 之後皆可):

```js
  function init() {
    currentLang = resolveInitialLang();
    validateTables();
    applyI18n();
  }
```

在它之前(或之後)插入:

```js
  function setLang(lang) {
    if (!SUPPORTED.includes(lang)) return;
    if (lang === currentLang) return;
    currentLang = lang;
    try { localStorage.setItem(STORAGE_KEY, lang); } catch (_) {}
    applyI18n();
  }
```

接著找 IIFE 末尾的 return 物件:
```js
  return {
    SUPPORTED,
    getLang: () => currentLang,
    t,
    applyI18n,
    init,
  };
```

加 setLang export:
```js
  return {
    SUPPORTED,
    getLang: () => currentLang,
    t,
    applyI18n,
    setLang,
    init,
  };
```

- [ ] **Step 3: 在 `index.html` 加 `.header-right` CSS**

找 `<style>` 區段內 `header { ... }` 那塊:

```css
    header {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      gap: 24px;
      flex-wrap: wrap;
      border-bottom: 1px dashed var(--line);
      padding-bottom: 24px;
      margin-bottom: 40px;
    }
```

在它之後插入 `.header-right` 與 `#lang-select`:

```css
    .header-right {
      display: flex;
      flex-direction: column;
      align-items: flex-end;
      gap: 8px;
    }
    #lang-select {
      font-size: 12px;
      padding: 4px 8px;
      width: auto;
    }
```

- [ ] **Step 4: 在 `index.html` 改 header HTML(加 dropdown,把 tagline 包進 .header-right)**

找:
```html
    <header>
      <h1>icon<span class="accent">.</span>machine</h1>
      <div class="tagline">
        <span data-i18n-key="app.tagline">程序化像素圖示產生器</span> <span class="blink"></span>
      </div>
    </header>
```

改成:
```html
    <header>
      <h1>icon<span class="accent">.</span>machine</h1>
      <div class="header-right">
        <select id="lang-select" aria-label="Language">
          <option value="zh-Hant">繁體中文</option>
          <option value="en">English</option>
        </select>
        <div class="tagline">
          <span data-i18n-key="app.tagline">程序化像素圖示產生器</span> <span class="blink"></span>
        </div>
      </div>
    </header>
```

> dropdown 的 `<option>` text 永遠顯示 native lang name,**不**走 i18n。`<select>` 的 `aria-label="Language"` 為固定英文(無障礙標籤,跨語言可接受;也可改 i18n,但 MVP 不做)。

- [ ] **Step 5: 在 `main.js` 綁 lang-select change event**

找 main.js 末尾的啟動序(Task 4 加的):

```js
// =============================================================
// 啟動
// =============================================================
I18N.init();
seedInput.value = generateRandomSeed();
generate();
renderBatchGrid();
```

改成(在 `I18N.init()` 後、`seedInput.value =` 前,或者整段啟動完之後 — 都行,選後者比較安全因為 dropdown DOM 都掛好;此處選「啟動完之後」):

```js
// =============================================================
// 啟動
// =============================================================
I18N.init();
seedInput.value = generateRandomSeed();
generate();
renderBatchGrid();

// =============================================================
// i18n dropdown
// =============================================================
const langSelect = document.getElementById('lang-select');
langSelect.value = I18N.getLang();
langSelect.addEventListener('change', (e) => {
  I18N.setLang(e.target.value);
});
```

- [ ] **Step 6: 跑驗證表達式 + UI 操作**

Reload `index.html`:
1. Console 跑 Step 1 表達式,全命中。
2. UI:header 右上應出現 dropdown(上)+ tagline(下)。
3. dropdown 切 English → 整頁文字即時切 en、`<title>` 切 en、seed input placeholder 切 en、4 個 type-select option 切 en。
4. 切回繁體中文 → 切 zh-Hant。
5. **重要:** 切語言過程 seed input 內容**不變**、batch grid 內容**不變**(canvas 不重畫)。

- [ ] **Step 7: Snapshot regression check**

```bash
npm run snapshot
git status snapshots/
```

期望:`snapshots/baseline/*.png` 完全不動,`snapshots/current/*.png` 跑出來跟 baseline byte-identical(snapshot 機制本身內部檢查)。
若 baseline 有任何變動 → bug,i18n 不應該影響 canvas。

- [ ] **Step 8: Commit**

```bash
git add i18n.js index.html main.js
git commit -m "$(cat <<'EOF'
feat(i18n): wire lang dropdown UI + setLang() for runtime switching

Header gets a lang-select dropdown (繁體中文 / English) with native
labels. setLang validates against SUPPORTED, persists to localStorage,
and re-applies i18n to DOM without reload. Canvas / batch grid
untouched on lang change.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: 建立 `scripts/check-i18n.mjs` static checker

**Files:**
- Create: `scripts/check-i18n.mjs`

Node script,user 可手動 `node scripts/check-i18n.mjs` 跑。比對 zh-Hant / en 兩 table 是否 key 一致,並 grep `index.html` 抓所有用到的 key,列出「HTML 用了但 table 沒定義」/「table 定義了但 HTML 沒用」。

- [ ] **Step 1: 定義驗證目標**

```bash
node scripts/check-i18n.mjs
# 期望(乾淨情境):
# Checking i18n consistency...
#   ✓ zh-Hant: 23 keys
#   ✓ en:      23 keys
#   ✓ Tables match
#   ✓ HTML uses 23 keys, all present in tables
# PASS
```

故意打破測:暫時在 `i18n/en.js` 註解掉 `'app.tagline': '...'`,跑:
```bash
node scripts/check-i18n.mjs
# 期望(失敗):
# Checking i18n consistency...
#   ✗ zh-Hant: 23 keys
#   ✗ en:      22 keys
#   ✗ Tables mismatch
#       Missing in en: [app.tagline]
# FAIL
# (exit code 1)
```

- [ ] **Step 2: 寫 `scripts/check-i18n.mjs`**

```js
#!/usr/bin/env node
// scripts/check-i18n.mjs
// Static integrity check for i18n. Diffs zh-Hant vs en key sets and grep
// index.html for data-i18n-key / data-i18n-attr-* references. Run via:
//   node scripts/check-i18n.mjs

import { readFile } from 'node:fs/promises';
import { fileURLToPath } from 'node:url';
import { dirname, resolve } from 'node:path';
import vm from 'node:vm';

const __filename = fileURLToPath(import.meta.url);
const __dirname  = dirname(__filename);
const ROOT       = resolve(__dirname, '..');

async function loadTable(relativePath, globalName) {
  const src = await readFile(resolve(ROOT, relativePath), 'utf8');
  const sandbox = { window: {} };
  vm.createContext(sandbox);
  vm.runInContext(src, sandbox);
  const table = sandbox.window[globalName];
  if (!table || typeof table !== 'object') {
    throw new Error(`failed to load ${globalName} from ${relativePath}`);
  }
  return table;
}

function diffKeys(a, b) {
  const aKeys = new Set(Object.keys(a));
  const bKeys = new Set(Object.keys(b));
  const missingInB = [...aKeys].filter((k) => !bKeys.has(k)).sort();
  const missingInA = [...bKeys].filter((k) => !aKeys.has(k)).sort();
  return { missingInA, missingInB };
}

async function extractHtmlKeys(htmlPath) {
  const src = await readFile(htmlPath, 'utf8');
  const used = new Set();
  // data-i18n-key="..."
  for (const m of src.matchAll(/data-i18n-key="([^"]+)"/g)) used.add(m[1]);
  // data-i18n-attr-<name>="..."  (name 可含 dash,例如 aria-label)
  for (const m of src.matchAll(/data-i18n-attr-[a-z-]+="([^"]+)"/g)) used.add(m[1]);
  return used;
}

async function main() {
  console.log('Checking i18n consistency...');
  let fail = false;

  const zh = await loadTable('i18n/zh-Hant.js', 'I18N_ZH_HANT');
  const en = await loadTable('i18n/en.js',      'I18N_EN');

  const zhCount = Object.keys(zh).length;
  const enCount = Object.keys(en).length;

  const { missingInA: missingInZh, missingInB: missingInEn } = diffKeys(zh, en);

  console.log(`  ${missingInEn.length === 0 ? '✓' : '✗'} zh-Hant: ${zhCount} keys`);
  console.log(`  ${missingInZh.length === 0 ? '✓' : '✗'} en:      ${enCount} keys`);

  if (missingInEn.length === 0 && missingInZh.length === 0) {
    console.log('  ✓ Tables match');
  } else {
    console.log('  ✗ Tables mismatch');
    if (missingInEn.length) console.log(`      Missing in en:      [${missingInEn.join(', ')}]`);
    if (missingInZh.length) console.log(`      Missing in zh-Hant: [${missingInZh.join(', ')}]`);
    fail = true;
  }

  const htmlKeys = await extractHtmlKeys(resolve(ROOT, 'index.html'));
  const tableKeys = new Set([...Object.keys(zh), ...Object.keys(en)]);

  const usedNotInTable = [...htmlKeys].filter((k) => !tableKeys.has(k)).sort();
  const definedNotUsed = [...tableKeys].filter((k) => !htmlKeys.has(k)).sort();

  if (usedNotInTable.length === 0) {
    console.log(`  ✓ HTML uses ${htmlKeys.size} keys, all present in tables`);
  } else {
    console.log(`  ✗ HTML uses keys not in tables: [${usedNotInTable.join(', ')}]`);
    fail = true;
  }

  if (definedNotUsed.length > 0) {
    console.log(`  ⚠ Defined but unused: [${definedNotUsed.join(', ')}]`);
    // warning, not failure
  }

  if (fail) {
    console.log('FAIL');
    process.exit(1);
  } else {
    console.log('PASS');
  }
}

main().catch((err) => {
  console.error(err);
  process.exit(2);
});
```

- [ ] **Step 3: 跑乾淨情境**

```bash
node scripts/check-i18n.mjs
```

期望輸出 PASS,exit code 0。

- [ ] **Step 4: 跑故意破壞情境**

暫時把 `i18n/en.js` 內 `'app.tagline': '...'` 那行註解掉:

```bash
node scripts/check-i18n.mjs
# 期望:
# Checking i18n consistency...
#   ✗ zh-Hant: 23 keys
#   ✗ en:      22 keys
#   ✗ Tables mismatch
#       Missing in en: [app.tagline]
# FAIL
echo "exit: $?"
# 期望:exit: 1
```

- [ ] **Step 5: 還原破壞**

把 `i18n/en.js` 註解恢復。再跑一次:
```bash
node scripts/check-i18n.mjs
```
期望 PASS。

- [ ] **Step 6: Commit**

```bash
git add scripts/check-i18n.mjs
git commit -m "$(cat <<'EOF'
chore(i18n): add scripts/check-i18n.mjs static consistency checker

Diffs zh-Hant/en key sets and greps index.html for data-i18n-* keys.
Reports missing-in-table (FAIL) and defined-but-unused (warn).
Manual run only — not wired into npm scripts or hooks per MVP scope.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: 全面 manual smoke test + snapshot regression 驗收

**Files:** (none — verification only)

Task 9 不寫 code,跑完整 acceptance check。任一情境 fail → 開新 task 修。

- [ ] **Step 1: 清狀態 + 跑 spec §10 風險清單對照**

```js
localStorage.removeItem('iconmachine.lang');
location.reload();
```

對照 spec [`docs/superpowers/specs/2026-05-10-i18n-design.md`](../specs/2026-05-10-i18n-design.md) §10 表 10 條 risk,逐條手測。記錄結果(commit message 內或新 doc)。

| # | 風險 | 驗證 | PASS/FAIL |
|---|---|---|---|
| 1 | `<select>` option 改 textContent 後 dropdown reflow | 切 lang → 開 type-select,選項顯示新 lang | |
| 2 | `<title>` 改 textContent 是否生效 | 切 lang,看分頁標題 | |
| 3 | localStorage 在 file:// 下行為 | 切 lang → reload,設定持續 | |
| 4 | URL `?lang=` invalid 不污染 localStorage | `?lang=fr` → DevTools Application 看 localStorage 應無 iconmachine.lang | |
| 5 | i18n.js missing 時 UI 不破 | 暫時註解 `<script src="i18n.js">` → batch grid 不 render(預期硬依賴失敗模式)、HTML zh-Hant 文字仍可見 | |
| 6 | navigator zh-CN → zh-Hant | 清 localStorage、無 URL,navigator 開頭 zh* → currentLang=zh-Hant | |
| 7 | 私密模式 localStorage throw | (Safari Private 視窗或 Chrome 隱私模式)切 lang 應仍能切,但不持久 | |
| 8 | type-select option textContent 換 lang 後 .value 不變 | DevTools 查 select.value | |
| 9 | applyI18n 在 mismatch 時仍跑 | 暫時刪 en 一個 key → console.error + UI 仍能顯示 fallback | |
| 10 | dropdown 切 lang 後 reroll 不帶副作用 | 切 lang → 點 reroll → 圖示變、種子變、lang 不變 | |

風險 #5 跟 #9 改完要記得**還原**(取消註解、補回 key)。

- [ ] **Step 2: 跑 snapshot regression**

```bash
npm run snapshot
git status snapshots/
```

期望:無 file 變動(`snapshots/baseline/*.png` 不動;`snapshots/current/` 是 gitignored,內部 byte-compare)。
若 baseline 變動 → bug,**不要 promote**,回去查 i18n 是否誤動 canvas / regression 檔。

- [ ] **Step 3: 跑 static checker**

```bash
node scripts/check-i18n.mjs
```

期望 PASS。

- [ ] **Step 4: 跑情境矩陣完整一輪**

對照 Task 5 Step 1 的測試矩陣 A~H,全跑一遍,全 PASS。

- [ ] **Step 5: 全機能 spot check**

zh-Hant 模式:
- [ ] generate 按鈕能產圖
- [ ] reroll 按鈕能換 seed
- [ ] download 按鈕能下載 png
- [ ] type-select 切 4 種 type 都能正確產對應物品
- [ ] size-select 切 16 / 32 都能正確產
- [ ] batch grid 點任一格能載入該 seed
- [ ] seed input enter 能 generate

切 en 模式重複以上。

- [ ] **Step 6: 確認 console 全程乾淨**

整段 manual smoke test 過程,console 不應出現任何 `[i18n]` warning / error(missing key、table mismatch 都不該有)。

- [ ] **Step 7: 收尾 commit(若 task 9 有 spot fix 才需要)**

若 Step 1-6 全 PASS、無代碼變動,跳過 commit。
若有發現需要修的,在 Task 9 內修完 commit。

```bash
# 範例(只在有變動時)
git add <files>
git commit -m "$(cat <<'EOF'
chore(i18n): smoke test fixes

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 8: 最終 git log 檢視**

```bash
git log --oneline -10
```

期望(從最舊到最新,大致):
```
fef51e2 docs(i18n): add design spec for zh-Hant/en i18n MVP
... feat(i18n): add zh-Hant and en translation tables (23 keys)
... feat(i18n): add window.I18N runtime skeleton with t() lookup
... feat(i18n): annotate index.html user-facing elements with data-i18n-* attrs
... feat(i18n): wire applyI18n() to DOM via I18N.init() at startup
... feat(i18n): implement resolveInitialLang chain (URL > localStorage > navigator)
... feat(i18n): add validateTables() dev-time integrity check
... feat(i18n): wire lang dropdown UI + setLang() for runtime switching
... chore(i18n): add scripts/check-i18n.mjs static consistency checker
```

---

## 完成準則(Definition of Done)

- ✅ Task 1-8 全部 commit 上 main
- ✅ Task 9 風險清單 10 條全 PASS
- ✅ `node scripts/check-i18n.mjs` PASS
- ✅ `npm run snapshot` 不打到 baseline
- ✅ Console 在預設啟動時乾淨無 warning / error
- ✅ Spec §11 列的所有「✅ 已決定」條目實際對應在 code 中
- ✅ HTML 內 zh-Hant fallback 文字保留(沒被 placeholder 化或刪除)

---

## 開放議題 / 後續工作(本輪不做)

- 加 ja / ko / zh-Hans 翻譯檔(純資料工作,需要 native reviewer)
- regression.html i18n
- npm script `check:i18n` + pre-commit hook
- 若 string 數量超過 ~80,考慮 build step + i18next 之類成熟工具
- Translation interpolation(`t('foo', {name})`)— 等實際遇到 dynamic string 再加
